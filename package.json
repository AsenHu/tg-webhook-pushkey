{
  "name": "tg-webhook-pushkey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "@cloudflare/workers-types": "^4.20241018.0",
    "typescript": "^5.5.2",
    "vitest": "2.1.3",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "generate-password": "^1.7.1"
  }
}